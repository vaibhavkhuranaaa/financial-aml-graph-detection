# Financial AML Graph Detection agent contract

Read `docs/STATE.md`, `docs/HANDOFF.md`, and fresh `graphify-out/` context before broad exploration. Inspect source directly when the graph does not cover the relevant files.

Use only public or properly licensed graph data and document its access terms. Never claim real-world illicit-activity detection, compliance suitability, evaluation performance, or deployment status without versioned evidence. Class imbalance requires precision/recall and operational error analysis rather than accuracy alone.

Preserve unrelated dirty work. Use purpose branches, conventional commits, and the configured human identity only; never add AI/model author or co-author attribution. Deployment, publication, merging, public visibility, and paid resources are separate owner-gated actions.

The project remains `building`; do not advance it until graph construction, evaluation, reproducibility, disclosure, and a safe demo are verified. Delegation is optional and must be bounded.
